# HSP TinyModel sidecar — TypeScript reference client

Copy these files into **Hyperlinks Space Program** as `ai/tinymodel.ts` (and types) when Phase 2 provider wiring begins. They mirror `scripts/phase3_reference_server.py` and `texts/phase3-serving-profile.md`.

**Env:** `TINYMODEL_API_URL` (default `http://127.0.0.1:8765`).

**Gates (TinyModel repo):** `python scripts/hsp_phase3_contract_smoke.py --verify` · `python scripts/hsp_reference_client_smoke.py --verify`
